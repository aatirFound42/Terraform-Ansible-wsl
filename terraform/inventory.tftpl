# terraform/inventory.tftpl
[nodes]
%{~ for idx, ip in ip_addrs ~}
node-${idx} ansible_host=${ip} ansible_user=vagrant ansible_ssh_private_key_file=${ssh_key} ansible_ssh_common_args='-o StrictHostKeyChecking=no'
%{~ endfor ~}

[nodes:vars]
ansible_python_interpreter=/usr/bin/python3
